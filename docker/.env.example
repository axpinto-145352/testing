# Gap Analysis Tool - Environment Configuration
# Copy to .env and fill in values before deployment
# WARNING: Never commit .env file with real credentials

# =============================================================================
# n8n Configuration
# =============================================================================
N8N_HOST=gap-analysis.yourdomain.com
N8N_ENCRYPTION_KEY=  # Generate: openssl rand -hex 32
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=  # Use strong password

# =============================================================================
# PostgreSQL Database
# =============================================================================
POSTGRES_USER=gap_analysis
POSTGRES_PASSWORD=  # Generate: openssl rand -base64 32

# =============================================================================
# Email / SMTP Configuration
# =============================================================================
NOTIFICATION_FROM_EMAIL=gap-analysis@yourdomain.com
ANALYST_EMAIL=analyst@yourdomain.com
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SSL=true

# =============================================================================
# Monitoring
# =============================================================================
GRAFANA_USER=admin
GRAFANA_PASSWORD=  # Use strong password

# =============================================================================
# Application URLs
# =============================================================================
DASHBOARD_URL=https://gap-analysis.yourdomain.com/grafana

# =============================================================================
# Security Notes
# =============================================================================
# - All passwords should be randomly generated (minimum 32 characters)
# - Use a secrets manager (HashiCorp Vault, AWS Secrets Manager) in production
# - Rotate credentials regularly (minimum every 90 days for CMMC compliance)
# - Never use default passwords in any environment
# - Ensure this file is in .gitignore
